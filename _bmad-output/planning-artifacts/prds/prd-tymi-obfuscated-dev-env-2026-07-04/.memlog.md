---
topic: TYMI v2 — Post-MVP PRD
status: complete
updated: 2026-07-04T22:14
---

- (direction) v2 scope: 11 candidate features across 3 clusters (scale-to-DB, streaming&contracts, deeper-chaos-monkey)
- (decision) Cross-job orchestration lives in an external scheduler (Airflow-style), NOT the library; core keeps only a thin in-process pipeline.py
- (decision) New v2 features must be new adapters/ports, core untouched (respect hexagonal + the 12 ADs, esp. AD-9 permissive deps)
- (direction) New chaos fault family requested: missing-data gaps (dropped rows, sequence holes, partial records, MCAR/MAR/MNAR) — distinct from illegal_null and missing_field
- (question) Open: which cluster to build first — pending the v2 forcing function (who/what is driving it)
- (insight) Market scan: content-chaos (missing-data/schema-drift, Cluster C) is the clearest white space; infra-chaos tools (Gremlin/Toxiproxy) and Great Expectations don't touch data content; schema/null-drift is the #1 cited pipeline pain
- (insight) Whole-DB gen (Cluster A) is crowded (SDV multi-table FK-aware, Tonic Fabricate) — generation half is catch-up; multi-table CHAOS is the novel part, best after C proves the engine
- (insight) Streaming/contract chaos (Cluster B): valid Kafka test-data + schema validation are commodity, but INVALID-payload-against-contract generation is unserved — real niche, higher connector cost
- (direction) Desk-research recommendation (input only): sequence C -> B -> A; A leapfrogs only if a design-partner needs whole-DB fixtures (GTM signal)
- (decision) Forcing function = internal company rollout; two jobs: (1) whole-DB obfuscated prod-like DEV env for collaborators, (2) break pipelines/alerts/streams with edge-cases
- (decision) Split into TWO PRDs; PRD 1 = Obfuscated Prod-Like Dev Environments (this run), PRD 2 = Pipeline & Stream Chaos (separate run later)
- (decision) Working mode = Coaching path; drafting PRD 1 first (the user's primary internal pull)
- (event) PROBLEM 1 (realism): nonprod data doesn't reflect prod distribution/behavior -> bugs don't reproduce
- (event) PROBLEM 2 (coverage): some tables don't even exist in nonprod -> can't build/test against the full schema
- (event) PROBLEM 3 (compliance, sharpest): devs/analysts copy REAL prod data down to lower envs to be able to work -> PII leaks downward; the tool kills this
- (event) PROBLEM 4 (cross-team consistency, novel): each team has different customer_ids in nonprod -> cross-project work needs constant manual ID alignment = wasted time
- (assumption) Determinism (AD-4 seed->byte-identical) implies a shared generation SPEC (Profile+seed+config) yields identical synthetic entities across teams/envs -> consistency-by-shared-spec could solve PROBLEM 4 without a central data store
- (decision) Consistency model = Option A: shared stable SYNTHETIC fakes via a shared spec+seed; NO traceability to real ids; keeps AD-6/AD-7 (zero real values, irreversible). Rejected Option B (keyed pseudonymization) as out of scope / non-user.
- (decision) REQUIREMENT (caveat): a controlled, immutable allow-list of specific login/test 'fixture' accounts must appear VERBATIM in the generated dev DB (people log in + test with them). Must be FK-consistent, exempt from obfuscation + leakage-gate regeneration, and shared across teams via the spec. They are synthetic-by-design fixtures, not real PII.
- (decision) Problem + Vision CONFIRMED by user (unrealistic/incomplete/unsafe/inconsistent nonprod -> one shared spec = whole-DB, prod-like, zero-real-value, cross-team-identical dev env with pinned fixture accounts)
- (decision) NORTH-STAR outcome: self-service, minutes-fast provisioning of prod-like data into nonprod WITHOUT a Data-Eng ticket and WITHOUT risking prod data. The win is removing the DE bottleneck + speed + safety, not fidelity/leakage per se (those are constraints).
- (assumption) Two-role model (to confirm): (a) Spec Author sets up the governed versioned whole-DB spec once (tables, sensitive cols, fixtures, correlations); (b) Self-service Consumer runs the spec to provision nonprod in minutes, no DE involvement. Removes DE from the common loop without losing governance.
- (override) DROP the two-role model (user rejected it — a separate 'spec author' role re-introduces a bottleneck, contradicting the north star). Correct model: ONE role = self-service consumer. The spec is a VERSIONED CONFIG ARTIFACT (spec-as-code in git), auto-bootstrapped by the tool (introspect + PII auto-classify -> first-cut spec), refined/approved via normal PR review. Governance lives in the file + review, not a gatekeeper person.
- (decision) User is ROLE-AGNOSTIC: data eng / dev / analyst — the persona is defined by the JOB (self-service provisioning), not the title. G1 reworded: the win is removing the mandatory gatekeeping HANDOFF (ticket/wait/cross-team coordination), not excluding DEs — a DE can also be the self-service user.
- (decision) Provisioning surface = CLI primitive (tymi provision --spec) wrapped by a CI/CD job OR Airflow DAG (self-service trigger), run on a CENTRAL RUNNER with source-read + dest-write access so the requester never handles prod credentials (NFR-6). Library ships the CLI/entrypoint, NOT the DAG (orchestration stays external, per the standing decision).
- (decision) SCOPE: cross-table statistical correlation IS IN v1 (user chose 'both' — FK-consistency AND cross-table correlation), overriding the coach's defer-it instinct. Accepted tradeoff: heavier PRD 1 for a strongly prod-like nonprod. (Note: this was a named MVP-PRD v2 item.)
- (decision) FR capability groups A-E confirmed (A whole-DB introspection+spec-as-code, B whole-DB faithful gen, C pinned fixtures, D cross-team consistency, E safe self-service provisioning)
- (decision) ADD group F (scale & refresh): FR-16 subsetting (referentially-consistent connected subset + sampling fraction, not a naive LIMIT), FR-17 incremental/delta refresh (update only what changed vs full rebuild). PM flag: FR-16 = connected-subset problem; FR-17 = highest-risk (delta must stay FK-consistent, preserve fixtures + determinism).
- (decision) REFRAME: this is an OPEN-SOURCE tool for everyone (the company is the design-partner/first user, not the only user). Scale target = NONE fixed; must span megabytes to hundreds of terabytes.
- (decision) Performance NFR (scalable decision, user delegated): expressed as bounded-memory THROUGHPUT, not a fixed time cap. Out-of-core/chunked generation (generate chunk -> load chunk -> free), bounded configurable memory footprint regardless of DB size; table-level parallelism respecting FK order; 'minutes' holds for small/medium DBs on one runner; large DBs scale ~linearly with resources. Distributed generation is the escape hatch for the extreme tail (deferred, same chunked design enables it).
- (decision) Reviewer gate ran (rubric strong 0C/0H/4M; adversarial MAJOR-REVISIONS 2 crit; feasibility CONDITIONAL). ADOPT PHASING + apply 5 autofixes (user approved).
- (decision) PHASING: Phase 1 = validate the cross-team-consistency wedge cheaply (small/in-memory whole-DB FK-consistent gen + leakage + fixtures + consistency; PDE-1..5,7..15). Phase 2 = out-of-core scale (NFR-A/B, the engine REWRITE). Phase 3 = cross-table correlation (PDE-6) + subsetting (PDE-16) + delta refresh (PDE-17).
- (decision) CONSISTENCY UNIT (fix CRIT-1): full data identity = (Spec + pinned Profile snapshot + seed + pinned deps). Entity keys are SOURCE-INDEPENDENT (seed-derived) so cross-team joins align even across source drift / different source snapshots. New OQ on key-generation-vs-fidelity tradeoff.
- (decision) FIXTURES fix (MAJOR-4): exempt from REGENERATION only, NOT from the leakage-gate/PII CHECK — fixture rows are still scanned and real PII is flagged (fail closed); logged attestation; synthetic fixtures preferred.
- (change) Renumber FRs to PDE-1..17 (was FR-1..17) to avoid collision with MVP FR-1..25.
- (change) Addendum updated with feasibility notes: engine rewrite for Phase-2 scale, leakage reference-set -> Bloom/disk-backed membership, BLAS-threading pin for determinism, single-hop cross-table correlation, reference-DB benchmark for G1a.
- (event) PRD 1 finalized (status: final). Reviewer gate applied: phasing adopted, consistency-unit + fixtures-PII + falsifiable-metrics + feasibility-honesty + FR-renumber. OQ-1..5 deferred to the architecture phase.
