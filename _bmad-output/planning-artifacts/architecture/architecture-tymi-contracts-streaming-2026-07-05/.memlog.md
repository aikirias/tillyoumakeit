---
scope: PRD 3 Contracts & Streaming — contract-invalid payload generation (wedge), StreamSink port + Pulsar/serialization, emit-boundary determinism
purpose: Build substrate: fix the invariants PRD 3's stories must share over inherited AD-1..34 (read-only)
altitude: feature (PRD → epics), 3 phases
updated: 2026-07-05T17:28
---

- (constraint) Inherited read-only: MVP AD-1..12 (hexagonal ports&adapters; AD-2 EngineAdapter BIDIRECTIONAL tabular/transactional; AD-3 entry-point extensibility tymi.engines/tymi.mutators; AD-4/AD-11 injected rng; AD-6/AD-7 leakage gate + zero real values; AD-9 permissive deps; AD-10 canonical Schema = DataFrame + FLAT column Schema, LogicalType has no nested type; AD-12 Evaluate chaos run_mode + FaultManifest). PRD1 AD-13..27, PRD2 AD-28..34.
- (constraint) Verified source (ports/__init__.py): Mutator.apply(dataset, *, rng) -> (Dataset, FaultManifest) is TABULAR; Exporter.export(dataset, *, target); EngineAdapter is bidirectional (introspect/sample/load/load_stream). Schema (domain/artifacts.py) is flat columns with LogicalType {INTEGER,FLOAT,BOOLEAN,STRING,DATETIME,...} — NO nested/object/array type. So an OpenAPI/JSON-Schema nested contract cannot be represented by the canonical Schema, and payload mutation cannot reuse the tabular Mutator port as-is.
- (constraint) import-linter forbids only core/ports/domain from importing adapters; adapter->adapter allowed (precedent synth->privacy). PRD3 adds NEW deps (unlike PRD1/2): pulsar-client/confluent-kafka Apache-2.0, pika BSD-3, fastavro MIT, protobuf BSD-3, jsonschema MIT, openapi-core BSD-3 — all permissive, web-verified 2026-07-04 in the PRD. New forbidden-for-core modules to add: tymi.contracts, tymi.sinks, tymi.serde.
- (decision) AD-35 Contracts are a NESTED domain, sibling to the tabular one — do NOT stretch AD-10. Binds SC-1/SC-2. Prevents: forcing a recursive OpenAPI/JSON-Schema contract into the flat column Schema (arrays, oneOf/anyOf, deep required, have no flat representation — and flattening loses exactly the structure the invalid-payload wedge must violate). Rule: SC-1 compiles a contract to a new ContractSchema (nested, constraint-bearing, -resolved via jsonschema referencing); a Payload artifact = one JSON document + its ContractSchema. Valid generation (SC-2) walks the ContractSchema tree and draws LEAF scalars via TYMI's existing marginal/faithful generators where a signature applies, else constraint-satisfying draws — 'builds on the generator' at the leaves, not the whole document. The flat canonical Schema (AD-10) stays the tabular representation; ContractSchema/Payload are new artifacts under the same core. [ASSUMPTION] chose Option B (nested artifact) over Option A (flatten-to-columns).
- (decision) AD-36 Contract-invalid chaos = a schema-aware PayloadMutator family; reuse the ENGINE PATTERNS, not the tabular Mutator port. Binds SC-3. Prevents: shoehorning nested-payload mutation into Mutator.apply(Dataset) (wrong artifact) OR forking a second bespoke discovery/chain/rng mechanism. Rule: a new PayloadMutator port (apply(payload, contract, *, rng) -> (Payload, FaultManifest)) discovered via a new entry-point group tymi.payload_mutators (mirroring AD-3), rng-threaded (AD-4/AD-11), manifest-merged like the tabular engine. Each invalid payload violates EXACTLY ONE contract constraint (type/range/enum/required/format/nesting) at a configured rate — surgical so the oracle attributes each rejection to one fault. Reuses the engine's discovery+chain+manifest+determinism patterns, not the Dataset-typed port.
- (decision) AD-37 Payload fault identity = (payload_index, JSON-pointer, constraint). Binds SC-3, G3. Prevents: reusing the tabular (row) or PRD2 (table,row_key) identity for a nested document where the violated thing is a path, not a row. Rule: a payload fault in the FaultManifest is keyed by the payload's ordinal in the emitted sequence + an RFC-6901 JSON-pointer to the violated location + the violated constraint id. The oracle/audit align on these. This is the payload analog of PRD2 AD-28's (table,row_key).
- (decision) AD-38 The validator is an INDEPENDENT pass/fail oracle in eval/, never in the generation path. Binds SC-3, G1. Prevents: a generator/mutator calling jsonschema to 'check its work' — that couples producer and judge and hides a mutator that fails to actually violate (or a valid payload that is accidentally invalid). Rule: jsonschema/openapi-core validate the emitted payloads as an independent oracle in eval/ (chaos run_mode, AD-12); it must REJECT EXACTLY the manifest's invalid set and ACCEPT the valid set (G1). The producer never imports the validator; the oracle never mutates. Separation of powers: produce vs judge.
- (decision) AD-39 StreamSink is a WRITE-ONLY destination port, separate from the bidirectional EngineAdapter. Binds SC-4, SC-7. Prevents: overloading EngineAdapter (tabular, transactional, introspect/sample/load) with stream semantics, or making a stream a bidirectional source (OQ-4 = emit-only). Rule: a new StreamSink port emits an ordered sequence of (topic, partition, key, value_bytes) records; NO introspect/sample (write-only); discovered via a new entry-point group tymi.sinks (AD-3 pattern). Pulsar first (SC-4), Kafka+RabbitMQ later (SC-7). A stream is never a profiling source — profiling stays batch (AD-2 unchanged for DBs).
- (decision) AD-40 Determinism SLA is the EMIT BOUNDARY, not broker state. Binds NFR-C, G3. Prevents: promising byte-identical topic state (broker CreateTime, offsets, compression framing, retry reordering are outside TYMI's control) — an unfalsifiable, unownable SLA. Rule: reproducibility = the deterministic ordered (partition, key, value_bytes) sequence + FaultManifest for a given seed+config. Requires: timestamps derived from the seed (not wall clock), the idempotent producer enabled, partitions explicitly pinned (never broker-hashed), canonical serialization (AD-41). The sink is judged on what it EMITS, not what the broker STORES.
- (decision) AD-41 Serialization maps from the canonical representation, canonicalized, with zero field loss. Binds SC-5, CM2. Prevents: serializing from raw pandas dtypes (AD-10 violation) or a lossy mapping that silently drops a field. Rule: serialize to JSON/Avro/Protobuf (all three, OQ-2) from the schema-of-record (flat Schema for tabular faithful streams; ContractSchema for contract payloads), canonicalized (sorted JSON keys / Avro canonical form / deterministic proto field order) so emit-boundary bytes are stable (AD-40); CM2 = a declared field that would be dropped on round-trip fails closed. Lives in a new tymi.serde module; the sink calls serde, serde maps from the schema.
- (decision) AD-42 Talk-to but don't-bundle a non-permissive SERVER; the registry is a client the sink uses. Binds SC-6, SC-7; extends AD-9. Prevents: TYMI shipping/redistributing/auto-provisioning a non-permissive server (Confluent Schema Registry server = Confluent Community License; RabbitMQ server = MPL-2.0). Rule: TYMI links only permissive CLIENTS and may CONNECT to any user-supplied broker/registry, but never bundles a non-permissive server. The Phase-2 reference registry is Pulsar's in-broker registry (Apache-2.0) — so Phase 2 is fully AD-9-clean; Confluent/Apicurio registries are user-supplied endpoints reached via permissive clients (Phase 3 with the Kafka sink).
- (version) New deps web-verified 2026-07-05: openapi-core 0.23.1 (BSD-3, OpenAPI 3.0/3.1/3.2, py>=3.10), pulsar-client 3.12.0 (Apache-2.0). Licenses for jsonschema (MIT), fastavro (MIT), protobuf (BSD-3), confluent-kafka (Apache-2.0), pika (BSD-3) web-verified 2026-07-04 in the PRD; all permissive (AD-9).
- (assumption) [ASSUMPTION] Structural seed: contracts/ (ingestion + ContractSchema + Payload + valid gen + PayloadMutator family), sinks/ (StreamSink impls, entry group tymi.sinks), serde/ (serializers). Oracle in eval/. New ports StreamSink + PayloadMutator in tymi.ports. import-linter forbidden-for-core += tymi.contracts, tymi.sinks, tymi.serde. Confirm module layout.
- (assumption) [ASSUMPTION] G1 acceptance margin reuses the inherited chaos margin (+/-2pp) on the configured invalid fraction; a single surgical violation per invalid payload (AD-36) makes the oracle's reject-set match the manifest exactly. Confirm the one-violation-per-payload rule.
- (question) OQ: SC-8 faithful stream provisioning reuses PRD1 provision to target a topic — does a tabular Dataset serialize row-per-message (each row -> one (key,value)), and is the leakage gate (AD-6/7) applied before serde on that faithful path? Leaning yes to both; flag for the epics.
- (decision) AD-43 G2 faithfulness = emit-boundary round-trip check, not a broker-state read. Binds SC-8/G2/CM2. Rule: deserialize the emitted value_bytes back through serde and compare to source field-by-field vs the schema-of-record (zero field loss); no live broker needed for the correctness claim (live-consumer read = integration test on top). Resolves the coverage gap (G2 had no owning AD) + the tension with AD-40. Leakage gate before serde on the faithful path.
- (decision) Gate tightening AD-35: honest reuse framing (constraint-only leaf generation is NET-NEW; reuse = Faker overlay + engine patterns, NOT Profile marginals since a contract has no Profile); ContractSchema canonical normal form ( kept as named nodes, cyclic bounded-unroll depth N, combinator canonical order) so two ingesters + their JSON-pointers + serialization agree. Closes adversarial holes 2/3 + both reviewers' 'reuse overstated'.
- (decision) Gate tightening AD-36: a 'violation' is DOCUMENT-LEVEL (whole doc must fail validation), not constraint-local; mutator fails closed on oneOf/anyOf/if-then/allOf-alternative targets unless the flip is provably document-invalidating (closes the 'accidentally-valid payload breaks G1' hole); CM1 = invalid set must span subtle/boundary near-misses. AD-40: partition = payload_index mod partition_count (fixed fn, not broker key-hash); keyless record key = seed-derived f(seed,payload_index), never null. AD-41: open-content (additionalProperties:true) serializes JSON-only or a declared map<string,..> catch-all (never silent drop); oneOf->union uses the canonical branch order (AD-35). AD-37 pointer rules: required->parent object, minItems/maxItems->array, per-item->index.
- (event) Reviewer gate: lint 0 findings; adversarial (6 holes) + reality/coverage (2 overstatements + G2/CM1 gaps) all resolved into AD-35/36/40/41 tightenings + new AD-43 + conventions. Honest reuse framing corrected (leaf gen net-new). Spine ready to finalize.
- (event) spine finalized
