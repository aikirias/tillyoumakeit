---
scope: PRD 2 Content Chaos — deeper chaos monkey (referential missing-data, cardinality-aware manifest, detection audit, MAR/MNAR conditioning, schema-drift depth)
purpose: Build substrate: fix the invariants PRD 2's stories must share, over the inherited MVP + PRD 1 spines (AD-1..27, read-only)
altitude: feature (PRD → epics), 3 phases
updated: 2026-07-05T16:47
---

- (constraint) Inherited read-only: MVP spine AD-1..12 (hexagonal ports&adapters; AD-3 Mutator/Engine via entry points; AD-4/AD-11 injected numpy RNG; AD-6/AD-7 leakage gate + zero real values; AD-9 permissive numpy/pandas only; AD-10 canonical Schema; AD-12 Evaluate chaos run_mode + FaultManifest).
- (constraint) Inherited read-only: PRD1 AD-13..27 (whole-DB Spec + generate_from_spec; AD-16 position-derived keys so every whole-DB table has a deterministic PK; AD-21 GatedDataset load boundary; substreams; cross-correlation/subset/delta). Chaos consumes the faithful whole-DB output (dict[table -> GatedDataset]).
- (constraint) import-linter: only core/ports/domain are forbidden from importing adapters; adapter->adapter IS allowed and already used (synth.whole_db imports tymi.privacy.classifier). So chaos may import synth.conditions without a new contract.
- (constraint) Verified source: chaos_audit._audit_cells marks any row-count change '(unauditable)' and diffs cells by FRAME POSITION (entry['row']=int index). FaultManifest entries are position-keyed, per-table (no table field). This is exactly what CC-8 must break. OrphanFkMutator/_drop_from_schema DO exist in chaos/mutators/schema_break.py (orphan_fk edits an FK cell value, preserves row count) — CC-1 dropped-records is genuinely new because it deletes whole rows (changes cardinality).
- (decision) AD-28 Row identity by stable key, not frame position. Binds CC-1/CC-3/CC-8/CC-11. Prevents: a positional manifest that can neither represent a deletion/insertion nor survive one (all lower rows shift). Rule: every FaultManifest value/cardinality entry identifies its row by (table, row_key) where row_key is the table's primary-key value (whole-DB tables always have one, AD-16); single-table fallback = a frozen origin-position label captured before any cardinality mutation. The audit aligns baseline<->chaotic rows by key: deletions = baseline_keys - chaotic_keys, insertions = chaotic_keys - baseline_keys, both first-class auditable; cell faults diff by key so surviving rows stay auditable after a sibling is dropped.
- (decision) AD-29 Cardinality faults run on a relational (whole-DB) chaos surface, not a per-table Mutator. Binds CC-1. Prevents: a single-table Mutator silently orphaning children across a join with no way to record/propagate the cross-table effect; unauditable RI corruption. Rule: row deletion/insertion is applied by a relational-chaos stage over the whole-DB dict[table -> Dataset] (reusing PRD1 whole-DB FK model + Schema.foreign_keys); dropping a parent row records a dropped_record on the parent AND the referential effect on orphaned children, all by (table,row_key). Per-table cell/schema Mutators (AD-3 engine) are unchanged and run within this surface. Deterministic compose order: cell/schema faults first, then cardinality (deletion last) so cell faults key-align before rows disappear.
- (decision) AD-30 Detection audit = a bidirectional diff over a declared consumer results file, living in eval/ (chaos run_mode, AD-12). Binds CC-11. Prevents: coupling the audit to any specific data-QA tool; an audit that only checks manifest-vs-output (Story 3.6) and can't answer 'did the consumer catch it'. Rule: consumes (FaultManifest, results file in JSON/YAML whose entries are keyed to the manifest fault vocabulary: fault_type/table/row_key/column) and emits detected (intersection), missed (manifest - results), false_alarm (results - manifest). Ships the schema + a mapping helper only; imports no GE/dbt/tool. Distinct from the Story-3.6 manifest audit (manifest faithfulness); this audits a third party's detection.
- (decision) AD-31 MAR/MNAR conditioned nullification reuses the shipped condition DSL (synth/conditions.py: Equals/Between/Members, a pure I/O-free parse/model/eval). Binds CC-2/CC-4. Prevents: a second, divergent condition parser inside chaos; a bespoke missingness grammar. Rule: a conditioned-nullification Mutator pairs a parsed Condition with a per-stratum null_rate (MAR = condition on ANOTHER column; MNAR = condition on the affected column's OWN value); cells outside the condition keep the run base rate. chaos imports synth.conditions directly (adapter->adapter, allowed, precedent synth->privacy) rather than forking or relocating the DSL. Nullification records per-cell null faults by (table,row_key,column) under AD-28.
- (decision) AD-32 Schema drift is a recorded delta from the canonical Schema (AD-10), not an ad-hoc mutation. Binds CC-5/CC-7. Prevents: type/nullability/order drift a contract test can't diff or version. Rule: schema-drift faults emit an explicit SchemaDelta artifact (ops: add/drop/rename/retype/nullability-change/REORDER columns) keyed off the AD-10 Schema, recorded in the FaultManifest structural channel so old->new is diffable declaratively. Column reorder (CC-5) is a delta op that permutes column order without changing the column set — the one column-level drift the MVP lacks. The existing _audit_structural is extended to recognize reorder + emit the delta.
- (decision) AD-33 The reference fixture is a self-contained example package OUTSIDE the hexagon. Binds CC-9/G1. Prevents: the toy pipeline/alerts leaking into tymi.* and becoming importable by core; a demo that depends on a real team. Rule: the synthetic reference fixture (multi-table customers/orders Spec + toy pipeline of deterministic transforms/joins + data-QA alert suite + a declared results file) lives as a self-contained package under examples/ (or tests/fixtures/reference/), consumes only the public library surface (Spec/provision/chaos/eval), is exercised in CI to demonstrate G1 (>=1 injected fault the pipeline/alerts miss), and is imported by NO tymi.* module.
- (assumption) [ASSUMPTION] Relational cardinality chaos is a STAGE in chaos/ that orchestrates over dict[table->Dataset] + the FK graph, NOT a new entry-point port. Per-table cell/schema faults keep the existing Mutator port (AD-3). Alt considered: a RelationalMutator port discovered by entry points (symmetric with AD-3) — deferred unless a 2nd relational fault family appears. Confirm.
- (assumption) [ASSUMPTION] Deterministic fault compose order = cell/schema faults first, cardinality (row delete/insert) LAST, so cell faults key-align before any row disappears; deletions recorded before the frame shrinks. Confirm the ordering.
- (assumption) [ASSUMPTION] The results file (CC-11) accepts BOTH JSON and YAML (PyYAML already in stack); one declared schema, keyed to fault_type/table/row_key/column. Confirm both formats.
- (assumption) [ASSUMPTION] conditions.py stays in synth/ and is imported by chaos in place (not promoted to domain), consistent with the existing synth->privacy adapter->adapter import. If a cleaner boundary is wanted, promote the pure DSL to tymi.domain. Confirm.
- (question) OQ: does the extended FaultManifest need an explicit schema_version bump (it changes shape: adds table + row_key + dropped_record/inserted_record fault_types)? Likely yes for reproducibility/versioning (G3) + the results-file contract.
- (question) OQ: for single-table (non-whole-DB) chaos with no PK, is the frozen origin-position row_key sufficient, or should CC-1/cardinality faults be whole-DB-only (require a Spec/PK)? Leaning: cardinality faults require a keyed table (PK or origin-position); pure position fallback only for cell faults.
- (decision) AD-34 Cardinality faults are Chaos-Policy-governed; faithful remainder stays faithful. Binds CC-8 policy half + CM2. Rule: relational-chaos stage driven by chaos/policy.py (extended); mixed mode = rate fraction eligible, rest byte-identical to faithful baseline (CM2); fully_chaotic over FKs breaks RI by design (inherited confirm guard); realized dropped fraction auditable vs rate via the manifest.
- (decision) Gate tightening AD-28: row_key is a FROZEN baseline label carried as the frame index (no Mutator port change), bound before any fault; PK-mutating faults record but never change identity; source = declared PK else AD-16 surrogate, fail closed if neither; duplicate_keys duplicated rows = audit-excuse region (identity is the frozen label, not the live PK). Closes adversarial A1/A4/A5 and the reality-check 'every table has a PK' overstatement.
- (decision) Gate tightening AD-29: orphaned children are LEFT DANGLING (not cascade-deleted) and recorded as a new 'orphaned_child' fault type; inserted rows mint row_keys from a reserved baseline-disjoint keyspace (mirror AD-16/17); tables visited FK-topological. Closes A2/A6.
- (decision) Gate tightening: canonical row_key encoding = JSON array of PK values in schema order, ints/floats normalized (closes A3); manifest sorted by (table,row_key,fault_type) before serialization + FK-topological iteration (closes A7/G3); AD-32 intra-delta op order rename->retype->nullability->reorder, reorder permutes post-rename names (closes A8); AD-31 records realized per-stratum null-rate to give G2 an architectural home.
- (event) Source reconciliation: PRD CC-1 'reuses OrphanFkMutator/_drop_from_schema' is factually wrong (neither deletes rows); spine notes the correction to 'reuses PRD1 whole-DB FK model' and will offer to update the PRD. Fixed phantom CC-6 in binds (PRD has no CC-6).
- (event) Reviewer gate: lint_spine 0 findings; adversarial lens (8 holes) + reality/coverage lens (3 gaps) all resolved into AD-28/29/31/32/34 + conventions. Spine ready to finalize.
- (event) spine finalized
